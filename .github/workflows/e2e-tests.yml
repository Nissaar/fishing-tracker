name: E2E Tests - Fishing Tracker Pro

on:
  pull_request:
    branches:
      - '*'
  push:
    branches:
      - main
      - develop

env:
  TEST_USER_EMAIL: e2etest@fishingtracker.mu
  TEST_USER_PASSWORD: E2ETestPassword123!
  TEST_ADMIN_EMAIL: admin@fishingtracker.mu
  TEST_ADMIN_PASSWORD: AdminPassword123!
  TEST_BASE_URL: http://localhost:80
  TEST_API_URL: http://localhost:5000/api

jobs:
  e2e-tests:
    name: Run E2E Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: testpassword
          POSTGRES_DB: fishing_tracker
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: |
            e2e/package-lock.json
            frontend/package-lock.json
            backend/package-lock.json

      - name: Install backend dependencies
        run: |
          cd backend
          npm ci

      - name: Install frontend dependencies
        run: |
          cd frontend
          npm ci

      - name: Install E2E test dependencies
        run: |
          cd e2e
          npm ci

      - name: Install Playwright browsers
        run: |
          cd e2e
          npx playwright install --with-deps chromium firefox

      - name: Initialize database
        run: |
          PGPASSWORD=testpassword psql -h localhost -U postgres -d fishing_tracker -f init.sql
        env:
          PGPASSWORD: testpassword

      - name: Create test users in database
        run: |
          PGPASSWORD=testpassword psql -h localhost -U postgres -d fishing_tracker << 'EOF'
          -- Create test user
          INSERT INTO users (username, email, password_hash, is_admin)
          VALUES (
            'E2E Test User',
            'e2etest@fishingtracker.mu',
            '$2a$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi',
            false
          ) ON CONFLICT (email) DO NOTHING;
          
          -- Create admin user
          INSERT INTO users (username, email, password_hash, is_admin)
          VALUES (
            'Admin User',
            'admin@fishingtracker.mu',
            '$2a$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi',
            true
          ) ON CONFLICT (email) DO NOTHING;
          EOF
        env:
          PGPASSWORD: testpassword

      - name: Create backend .env file
        run: |
          cat << EOF > backend/.env
          NODE_ENV=test
          PORT=5000
          DB_HOST=localhost
          DB_PORT=5432
          DB_NAME=fishing_tracker
          DB_USER=postgres
          DB_PASSWORD=testpassword
          JWT_SECRET=test-jwt-secret-for-e2e-testing
          SESSION_SECRET=test-session-secret
          CORS_ORIGIN=http://localhost:3000,http://localhost:80
          EOF

      - name: Create frontend .env file
        run: |
          cat << EOF > frontend/.env
          REACT_APP_API_URL=http://localhost:5000/api
          EOF

      - name: Start backend server
        run: |
          cd backend
          npm start &
          sleep 10
        env:
          NODE_ENV: test

      - name: Build and start frontend
        run: |
          cd frontend
          npm run build
          npx serve -s build -l 80 &
          sleep 5

      - name: Wait for services to be ready
        run: |
          echo "Waiting for backend..."
          timeout 60 bash -c 'until curl -s http://localhost:5000/health > /dev/null; do sleep 2; done'
          echo "Backend is ready!"
          
          echo "Waiting for frontend..."
          timeout 60 bash -c 'until curl -s http://localhost:80 > /dev/null; do sleep 2; done'
          echo "Frontend is ready!"

      - name: Run Playwright tests
        run: |
          cd e2e
          npx playwright test --reporter=html,github,json
        env:
          CI: true
          TEST_BASE_URL: http://localhost:80
          TEST_API_URL: http://localhost:5000/api

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: e2e/playwright-report/
          retention-days: 30

      - name: Upload test artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: |
            e2e/test-results/
          retention-days: 7

      - name: Generate test summary
        if: always()
        run: |
          cd e2e
          if [ -f test-results/results.json ]; then
            echo "## 🧪 E2E Test Results" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # Parse JSON results
            PASSED=$(jq -r '.stats.expected // 0' test-results/results.json)
            FAILED=$(jq -r '.stats.unexpected // 0' test-results/results.json)
            SKIPPED=$(jq -r '.stats.skipped // 0' test-results/results.json)
            DURATION=$(jq -r '.stats.duration // 0' test-results/results.json)
            
            echo "| Metric | Value |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| ✅ Passed | $PASSED |" >> $GITHUB_STEP_SUMMARY
            echo "| ❌ Failed | $FAILED |" >> $GITHUB_STEP_SUMMARY
            echo "| ⏭️ Skipped | $SKIPPED |" >> $GITHUB_STEP_SUMMARY
            echo "| ⏱️ Duration | ${DURATION}ms |" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📊 Full report available in the artifacts" >> $GITHUB_STEP_SUMMARY
          else
            echo "## ⚠️ No test results found" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Cleanup test data
        if: always()
        run: |
          PGPASSWORD=testpassword psql -h localhost -U postgres -d fishing_tracker << 'EOF'
          -- Clean up test data
          DELETE FROM fishing_logs WHERE notes LIKE 'E2E_TEST_%';
          DELETE FROM custom_submissions WHERE value LIKE 'E2E%' OR value LIKE '%E2E Test%';
          DELETE FROM contact_messages WHERE name LIKE 'E2E_TEST_%';
          EOF
        env:
          PGPASSWORD: testpassword
        continue-on-error: true

  test-report:
    name: Publish Test Report
    needs: e2e-tests
    if: always()
    runs-on: ubuntu-latest

    steps:
      - name: Download test results
        uses: actions/download-artifact@v4
        with:
          name: playwright-report
          path: playwright-report

      - name: Deploy report to GitHub Pages
        if: github.ref == 'refs/heads/main'
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./playwright-report
          destination_dir: test-reports/${{ github.run_id }}

      - name: Comment PR with test results
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            
            let summary = '## 🧪 E2E Test Results\n\n';
            
            try {
              const resultsPath = 'test-results/results.json';
              if (fs.existsSync(resultsPath)) {
                const results = JSON.parse(fs.readFileSync(resultsPath, 'utf8'));
                
                const passed = results.stats?.expected || 0;
                const failed = results.stats?.unexpected || 0;
                const skipped = results.stats?.skipped || 0;
                
                summary += `| Status | Count |\n`;
                summary += `|--------|-------|\n`;
                summary += `| ✅ Passed | ${passed} |\n`;
                summary += `| ❌ Failed | ${failed} |\n`;
                summary += `| ⏭️ Skipped | ${skipped} |\n\n`;
                
                if (failed > 0) {
                  summary += `⚠️ **${failed} test(s) failed.** Please check the test report for details.\n`;
                } else {
                  summary += `✅ **All tests passed!**\n`;
                }
              } else {
                summary += 'Test results not found. Check the workflow logs for details.\n';
              }
            } catch (error) {
              summary += `Error reading test results: ${error.message}\n`;
            }
            
            summary += '\n📊 [View Full Test Report](../actions/runs/${{ github.run_id }})\n';
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: summary
            });
